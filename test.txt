git clone https://github.com/nabilanoof/stratgit.git

git init

git add .

git commit -m "filename"

git remote add origin "gitlink"

git remote set-url origin "link"

git push 1) username 2) token

Branches :
git checkout -b office
git pull origin ...
git push origin ...
switch to branch : git checkout "name"
git merge "branch"